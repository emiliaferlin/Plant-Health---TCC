import 'package:flutter/material.dart';

class SairView extends StatelessWidget {
  const SairView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Sair", style: TextStyle(fontSize: 24)));
  }
}
